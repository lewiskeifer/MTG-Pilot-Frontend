import { Component } from '@angular/core';

/** The singles page: deck and card tables. Every graph lives on the dashboard. */
@Component({
  selector: 'app-decks',
  templateUrl: './decks.component.html',
  styleUrls: ['./decks.component.scss'],
  standalone: false
})
export class DecksComponent {}
